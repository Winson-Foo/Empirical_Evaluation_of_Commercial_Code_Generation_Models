/*
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

// @generated by enums.py

package com.facebook.yoga;

import java.util.HashMap;
import java.util.Map;

/**
 * Defines how child nodes are aligned within a Yoga node.
 */
public enum YogaAlign {
  AUTO(0),
  FLEX_START(1),
  CENTER(2),
  FLEX_END(3),
  STRETCH(4),
  BASELINE(5),
  SPACE_BETWEEN(6),
  SPACE_AROUND(7);

  private final int value;
  private static final Map<Integer, YogaAlign> map = new HashMap<>();

  YogaAlign(int value) {
    this.value = value;
  }

  /**
   * Get the integer value associated with this enum value.
   */
  public int intValue() {
    return value;
  }

  /**
   * Get the enum value associated with the given integer value.
   * @throws IllegalArgumentException if the input value is not a valid enum value
   */
  public static YogaAlign fromInt(int value) {
    YogaAlign align = map.get(value);
    if (align == null) {
      throw new IllegalArgumentException("Unknown enum value: " + value);
    }
    return align;
  }

  // Static initialization block to populate the map
  static {
    for (YogaAlign align : YogaAlign.values()) {
      map.put(align.value, align);
    }
  }
}

